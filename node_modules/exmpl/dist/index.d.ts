import { ReactNode, ReactChild } from 'react';
export { Code } from './markup/Code';
export { Button } from './markup/Button';
export { Input } from './markup/Input';
export { Grid } from './markup/Grid';
export { Tabs } from './markup/Tabs';
interface Props {
    title?: string | ReactChild;
    npm?: string;
    github?: string;
    icons?: ReactChild;
    children: ReactNode;
}
export declare const Exmpl: ({ title, npm, github, icons, children, }: Props) => JSX.Element;
