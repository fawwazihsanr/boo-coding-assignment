import React from 'react';
declare type DivProps = React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement>;
declare type CustomDivProps = Omit<DivProps, 'onChange'>;
declare type Props = {
    labels: string[];
    initialTab?: number;
    onChange?: (label: string, index: number) => void;
} & CustomDivProps;
export declare const Tabs: ({ labels, children, initialTab, style, onChange, ...props }: Props) => JSX.Element;
export {};
